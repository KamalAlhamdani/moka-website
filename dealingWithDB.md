# Delete users and their actions

```sql


TRUNCATE `claims`;

TRUNCATE `disposits`;
TRUNCATE `balance_transactions`;
TRUNCATE `user_wallets`;

TRUNCATE `hospitalities`;

TRUNCATE `rate_details`;
TRUNCATE `rates`;

TRUNCATE `requests`;

TRUNCATE `cart_details`;
TRUNCATE `carts`;

TRUNCATE `coupons`;

TRUNCATE `special_products`;
TRUNCATE `system_notifications`;

TRUNCATE `user_addresses`;
TRUNCATE `user_events`;
TRUNCATE `user_favorites`;
TRUNCATE `user_fire_base_tokens`;

TRUNCATE `users`;
```
